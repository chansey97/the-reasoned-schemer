#lang racket

;; TODO: Anonymous variable:

;; When need anonymous variable _ , use (var (quote )) directly (that means each time create a new variable)

;; see 1ed p.131, we can do it by a identifier macro.
